apply plugin: 'java'


jar {
    manifest {
        attributes 'Implementation-Title': "Tommy McCormick's Gradle Build",
                'Implementation-Version': "1.0"
    }
}

dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
}